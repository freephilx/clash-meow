# Mihomo Runtime

This directory is populated from `RuntimeLocks/mihomo.lock.json` during the
Xcode build. Generated binaries, GeoData, manifests, and license files are not
stored in Git.

Prepare the verified local cache before building:

```bash
scripts/prepare-mihomo-runtime.sh --arch all
```

